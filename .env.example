PORT=5000
FRONTEND_URL=http://localhost:3000
# Optional: paste a Mistral API key (console.mistral.ai) for real paper reading.
# Without it, the server still responds but marks results for manual review.
MISTRAL_API_KEY=
MISTRAL_MODEL=pixtral-12b-2409
